# Todo App Example

This directory is reserved for future Dure examples.

In v0.1, the CLI uses deterministic mock agents and produces PatchProposal summaries instead of modifying example files automatically.
